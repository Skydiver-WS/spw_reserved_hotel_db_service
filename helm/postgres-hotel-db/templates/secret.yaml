apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
data:
  POSTGRES_USERNAME: {{ .Values.secret.username | b64enc }}
  POSTGRES_PASSWORD: {{ .Values.secret.password | b64enc }}
  POSTGRES_DB: {{ .Values.secret.db | b64enc }}